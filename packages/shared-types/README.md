# @tennisillo/shared-types

Types and constants shared across `apps/web`, `apps/api`, and engine packages.

Keep this package lean — only types that are genuinely used by ≥2 consumers.
Framework-specific types stay in their respective app/package.
