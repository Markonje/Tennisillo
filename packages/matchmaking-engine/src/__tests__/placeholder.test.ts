// Sprint 5 will replace this with full scorer tests (≥90% coverage required).
// See: docs/specs/02_specifiche_sviluppo.md §6

describe('matchmaking-engine', () => {
  it('exports are defined (placeholder)', () => {
    // eslint-disable-next-line @typescript-eslint/no-require-imports
    const mod = require('../index') as Record<string, unknown>;
    expect(mod).toBeDefined();
  });
});
